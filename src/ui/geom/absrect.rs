
// A viewport in absolute screen coordinates measured in pixels (ready for Vulkan)
#[derive(Debug, Clone)]
pub struct AbsRect {
    pub x: f32,
    pub y: f32,
    pub width: f32,
    pub height: f32,
}
